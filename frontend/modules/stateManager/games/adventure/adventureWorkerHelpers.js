import { GameWorkerHelpers } from '../../helpers/gameWorkerHelpers.js';

export class AdventureWorkerHelpers extends GameWorkerHelpers {
  constructor(manager) {
    super(manager);
    // console.log('[AdventureWorkerHelpers] Initialized.');
  }
  // Adventure-specific worker helper methods will go here (if any).
}
