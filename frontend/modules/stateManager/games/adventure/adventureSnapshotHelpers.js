import { GameSnapshotHelpers } from '../../helpers/gameSnapshotHelpers.js';

export class AdventureSnapshotHelpers extends GameSnapshotHelpers {
  constructor(snapshotInterface) {
    super(snapshotInterface);
    // console.log('[AdventureSnapshotHelpers] Initialized.');
  }
  // Adventure-specific snapshot helper methods will go here (if any).
}
