import { GameWorkerHelpers } from '../../helpers/gameWorkerHelpers.js';

export class AHatInTimeWorkerHelpers extends GameWorkerHelpers {
  constructor(manager) {
    super(manager);
    // console.log('[AHatInTimeWorkerHelpers] Initialized.');
  }
  // A Hat in Time-specific worker helper methods will go here.
}
