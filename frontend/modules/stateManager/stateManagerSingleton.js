// Import the StateManager class from the same directory
import { StateManager } from './stateManager.js';

// Create and export the singleton instance
const stateManagerSingleton = new StateManager();
export default stateManagerSingleton;
